set -g default-terminal "screen-256color"
set-window-option -g mode-keys vi

# http://alexyoung.org/2011/12/19/build-an-ide-with-tmux-and-vim/
#
bind h select-pane -L
bind j select-pane -D
bind k select-pane -U
bind l select-pane -R

# http://robots.thoughtbot.com/post/2166174647/love-hate-tmux?630b7c80
#
unbind C-b
set -g prefix C-x

# https://github.com/ChrisJohnsen/tmux-MacOSX-pasteboard
set-option -g default-command "reattach-to-user-namespace -l bash"

# reload config
#
bind r source-file ~/.tmux.conf \; display "Reloaded!"

# split windows
#
bind | split-window -h -c '#{pane_current_path}'
bind - split-window -v -c '#{pane_current_path}'

# open new window in current dir
#
bind c new-window -c '#{pane_current_path}'

# resize panes
#
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5

# improve response
#
set -sg escape-time 1

# copy and paste
#
unbind [
bind Escape copy-mode
unbind p
bind p paste-buffer
bind -t vi-copy 'v' begin-selection
bind -t vi-copy 'y' copy-selection

# system copy and paste
#
bind C-c run "tmux save-buffer - | reattach-to-user-namespace pbcopy"
bind C-v run "tmux set-buffer $(reattach-to-user-namespace pbpaste); tmux paste-buffer"

# Clear Screen
#
bind-key -n C-l send-keys "clear && tmux clear-history" \; send-keys "Enter"

# Powerline
#
set-option -g status on
set-option -g status-interval 2
set-option -g status-justify "centre"
set-option -g status-left-length 89
set-option -g status-right-length 90
set-option -g status-left "#(~/Development/dotfiles/tmux/plugins/tmux-powerline/powerline.sh left)"
set-option -g status-right "#(~/Development/dotfiles/tmux/plugins/tmux-powerline/powerline.sh right)"
